[project]
name = "classflow-backend"
version = "1.0.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.pyright]
venvPath = ".."
venv = "venv"
extraPaths = [".."]
